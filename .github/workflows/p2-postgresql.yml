name: P2 PostgreSQL append integrity

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'native_kernel/postgresql_profile/**'
      - 'native_kernel/profile_common/**'
      - 'native_kernel/semantic_core/**'
      - 'profiles/postgresql-reference-v0/p2-manifest.json'
      - 'profiles/postgresql-reference-v0/requirements-p2-ci.txt'
      - 'tools/profiles/validate_p2_manifest.py'
      - 'tests/test_postgresql_profile_*.py'
      - 'tests/test_profile_event_envelope_contract.py'
      - 'tests/test_p2_manifest.py'
      - '.github/workflows/p2-postgresql.yml'
  push:
    branches: [main]
    paths:
      - 'native_kernel/postgresql_profile/**'
      - 'native_kernel/profile_common/**'
      - 'native_kernel/semantic_core/**'
      - 'profiles/postgresql-reference-v0/p2-manifest.json'
      - 'profiles/postgresql-reference-v0/requirements-p2-ci.txt'
      - 'tools/profiles/validate_p2_manifest.py'
      - 'tests/test_postgresql_profile_*.py'
      - 'tests/test_profile_event_envelope_contract.py'
      - 'tests/test_p2_manifest.py'
      - '.github/workflows/p2-postgresql.yml'

permissions:
  contents: read

jobs:
  p2-postgresql:
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        python: ['3.11', '3.12']
        postgres: ['16', '18']
    services:
      postgres:
        image: postgres:${{ matrix.postgres }}
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: native_kernel_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d native_kernel_test"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    env:
      NK_TEST_POSTGRES_DSN: postgresql://postgres:postgres@127.0.0.1:5432/native_kernel_test
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: ${{ matrix.python }}
      - name: Install P2 integration driver
        run: python -m pip install "psycopg[binary]==3.3.2"
      - name: Unit tests
        run: |
          python -m unittest discover -s tests -p 'test_postgresql_profile_unit.py' -v
          python -m unittest discover -s tests -p 'test_profile_event_envelope_contract.py' -v
      - name: Manifest guards
        run: |
          python -m unittest discover -s tests -p 'test_p2_manifest.py' -v
          python tools/profiles/validate_p2_manifest.py
      - name: PostgreSQL integration tests
        run: python -m unittest discover -s tests -p 'test_postgresql_profile_integration.py' -v
      - name: Compile packages
        run: python -m compileall -q native_kernel tools/profiles
